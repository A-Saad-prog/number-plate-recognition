# Root project dependency file
# Install backend Python dependencies with:
#   python -m pip install -r requirements.txt
# Install frontend Node dependencies with:
#   cd frontend
#   npm install

-r backend/requirements.txt
