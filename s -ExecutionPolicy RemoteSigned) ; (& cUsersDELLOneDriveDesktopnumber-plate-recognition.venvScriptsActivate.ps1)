warning: in the working copy of 'frontend/src/pages/AdminPage.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/frontend/src/pages/AdminPage.jsx b/frontend/src/pages/AdminPage.jsx[m
[1mindex 0959578..f77c09d 100644[m
[1m--- a/frontend/src/pages/AdminPage.jsx[m
[1m+++ b/frontend/src/pages/AdminPage.jsx[m
[36m@@ -5,14 +5,18 @@[m [mimport {[m
     getAdminSession,[m
     getAdminSettings,[m
     getParkingActivity,[m
[32m+[m[32m    getAnalytics,[m
     getWhitelist,[m
     loginAdmin,[m
     removeWhitelistEntry,[m
     saveBillingConfig,[m
     saveCameraConfig,[m
     saveGarageSettings,[m
[32m+[m[32m    removeParkingSession,[m
[32m+[m[32m    updateParkingVehicle,[m
 } from "../services/api";[m
 import "../styles/AdminPage.css";[m
[32m+[m[32mimport { localPlateImageSupport, savedPlateImageFolderName, selectPlateImageFolder } from "../services/localPlateImages";[m
 [m
 const TOKEN_KEY = "parking_admin_token";[m
 const LANGUAGE_KEY = "parking_admin_language";[m
[36m@@ -88,6 +92,7 @@[m [mfunction AdminPage() {[m
     const [submitting, setSubmitting] = useState(false);[m
     const [error, setError] = useState("");[m
     const [activeFeature, setActiveFeature] = useState(null);[m
[32m+[m[32m    const [accountMenuOpen, setAccountMenuOpen] = useState(false);[m
     const [plate, setPlate] = useState("");[m
     const [vehicleName, setVehicleName] = useState("");[m
     const [discount, setDiscount] = useState("");[m
[36m@@ -100,7 +105,9 @@[m [mfunction AdminPage() {[m
     const whitelistFetchInFlightRef = useRef(false);[m
     const [whitelistError, setWhitelistError] = useState("");[m
     const [whitelistMessage, setWhitelistMessage] = useState("");[m
[31m-    const [garageSettings, setGarageSettings] = useState({ level_count: "", levels: [], spaces_per_level: "" });[m
[32m+[m[32m    const [garageSettings, setGarageSettings] = useState({ mode: "parking", level_count: "", levels: [], spaces_per_level: "" });[m
[32m+[m[32m    const [localImageFolder, setLocalImageFolder] = useState("");[m
[32m+[m[32m    const [localImageStatus, setLocalImageStatus] = useState("");[m
     const [garageSettingsMessage, setGarageSettingsMessage] = useState("");[m
     const [garageSettingsAlreadyApplied, setGarageSettingsAlreadyApplied] = useState(false);[m
     const [savedGarageSettings, setSavedGarageSettings] = useState(null);[m
[36m@@ -131,13 +138,17 @@[m [mfunction AdminPage() {[m
     const [confirmationOpen, setConfirmationOpen] = useState(false);[m
     const [confirmationSection, setConfirmationSection] = useState(null);[m
     const [settingsSubmitting, setSettingsSubmitting] = useState(null);[m
[31m-    const [billingConfig, setBillingConfig] = useState({ payments_enabled: false, cash_enabled: false, card_enabled: false, rate_per_minute: 1.67 });[m
[32m+[m[32m    const [billingConfig, setBillingConfig] = useState({ payments_enabled: false, cash_enabled: false, card_enabled: false, rate_per_minute: 1.67, rate_unit: "minute" });[m
     const [billingMessage, setBillingMessage] = useState("");[m
     const [billingRateError, setBillingRateError] = useState("");[m
     const [savedBillingConfig, setSavedBillingConfig] = useState(null);[m
     const [parkingActivity, setParkingActivity] = useState(null);[m
     const [activityLoading, setActivityLoading] = useState(false);[m
     const [activityError, setActivityError] = useState("");[m
[32m+[m[32m    const [analytics, setAnalytics] = useState(null);[m
[32m+[m[32m    const [analyticsMetric, setAnalyticsMetric] = useState("earnings");[m
[32m+[m[32m    const [analyticsPeriod, setAnalyticsPeriod] = useState("7d");[m
[32m+[m[32m    const [activeSessionMenuId, setActiveSessionMenuId] = useState(null);[m
     const activityLoadingRef = useRef(false);[m
 [m
     useEffect(() => {[m
[36m@@ -182,7 +193,8 @@[m [mfunction AdminPage() {[m
                 const garage = settings.garage_settings;[m
                 if (garage?.level_count > 0) {[m
                     const normalizedGarageSettings = {[m
[31m-                        level_count: String(garage.level_count),[m
[32m+[m[32m                        mode: garage.mode || "parking", level_count: String(garage.level_count),[m
[32m+[m[32m                        local_image_saving: Boolean(garage.local_image_saving),[m
                         spaces_per_level: String(garage.spaces_per_level),[m
                         automatic_entry: Boolean(garage.automatic_entry),[m
                         levels: (garage.levels || []).map((level) => ({[m
[36m@@ -203,9 +215,9 @@[m [mfunction AdminPage() {[m
                     setSavedCameraSetup(normalizeCameraSetup(normalizedCameraConfig, cameraAssignments));[m
                 }[m
                 if (settings.billing_config) {[m
[31m-                    const normalizedBillingConfig = {[m
[32m+[m[32m            const normalizedBillingConfig = {[m
                         ...settings.billing_config,[m
[31m-                        rate_per_minute: settings.billing_config.rate_per_minute ?? 1.67,[m
[32m+[m[32m                        rate_per_minute: settings.billing_config.rate_per_minute ?? 1.67, rate_unit: settings.billing_config.rate_unit || "minute",[m
                     };[m
                     setBillingConfig(normalizedBillingConfig);[m
                     setSavedBillingConfig(normalizeBillingConfig(normalizedBillingConfig));[m
[36m@@ -213,6 +225,7 @@[m [mfunction AdminPage() {[m
             })[m
             .catch(() => { });[m
     }, [token]);[m
[32m+[m[32m    useEffect(() => { savedPlateImageFolderName().then((name) => setLocalImageFolder(name || "")).catch(() => {}); }, []);[m
     useEffect(() => {[m
         setGarageSettingsAlreadyApplied([m
             isGarageSettingsAlreadyApplied([m
[36m@@ -266,10 +279,21 @@[m [mfunction AdminPage() {[m
         if (activeFeature !== "parking-activity" || !token) return undefined;[m
         const interval = window.setInterval(() => {[m
             void loadParkingActivity();[m
[31m-        }, 2500);[m
[32m+[m[32m        }, 10000);[m
         return () => window.clearInterval(interval);[m
     }, [activeFeature, token]);[m
 [m
[32m+[m[32m    useEffect(() => {[m
[32m+[m[32m        const closeMenus = (event) => {[m
[32m+[m[32m            if (!event.target.closest(".account-menu")) setAccountMenuOpen(false);[m
[32m+[m[32m            if (!event.target.closest(".vehicle-menu")) setActiveSessionMenuId(null);[m
[32m+[m[32m        };[m
[32m+[m[32m        const closeOnEscape = (event) => { if (event.key === "Escape") { setAccountMenuOpen(false); setActiveSessionMenuId(null); } };[m
[32m+[m[32m        document.addEventListener("mousedown", closeMenus);[m
[32m+[m[32m        document.addEventListener("keydown", closeOnEscape);[m
[32m+[m[32m        return () => { document.removeEventListener("mousedown", closeMenus); document.removeEventListener("keydown", closeOnEscape); };[m
[32m+[m[32m    }, []);[m
[32m+[m
     async function submitWhitelist(event) {[m
         event.preventDefault();[m
         setWhitelistLoading(true);[m
[36m@@ -351,6 +375,29 @@[m [mfunction AdminPage() {[m
         }[m
     }[m
 [m
[32m+[m[32m    async function chooseLocalImageFolder() {[m
[32m+[m[32m        try { setLocalImageFolder(await selectPlateImageFolder()); setLocalImageStatus("Folder ready for confirmed plate images."); }[m
[32m+[m[32m        catch (error) { setLocalImageStatus(error?.message || "Folder selection was cancelled."); }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    async function removeLiveSession(sessionId) {[m
[32m+[m[32m        if (!window.confirm("Remove this vehicle from parking and free its space?")) return;[m
[32m+[m[32m        try { await removeParkingSession(token, sessionId); await loadParkingActivity(); }[m
[32m+[m[32m        catch (err) { setActivityError(err.message || "Unable to remove parking."); }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    async function editLiveSession(session) {[m
[32m+[m[32m        const nextPlate = window.prompt("Number plate", session.plate);[m
[32m+[m[32m        if (!nextPlate || nextPlate.trim().toUpperCase() === session.plate) return;[m
[32m+[m[32m        try { await updateParkingVehicle(token, session.session_id, nextPlate.trim().toUpperCase()); await loadParkingActivity(); }[m
[32m+[m[32m        catch (err) { setActivityError(err.message || "Unable to update vehicle."); }[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    async function loadAnalytics(period = analyticsPeriod) {[m
[32m+[m[32m        try { setAnalytics(await getAnalytics(token, period)); }[m
[32m+[m[32m        catch (err) { setActivityError(err.message || "Unable to load analytics."); }[m
[32m+[m[32m    }[m
[32m+[m
     function showWhitelist() {[m
         if (whitelistVisible) {[m
             setWhitelistVisible(false);[m
[36m@@ -376,6 +423,7 @@[m [mfunction AdminPage() {[m
 [m
     function handleCameraConfigChange(field, value) {[m
         const nextValue = value.replace(/[^\d]/g, "");[m
[32m+[m[32m        const nextConfig = { ...cameraConfig, [field]: nextValue };[m
         setCameraConfig((current) => ({[m
             ...current,[m
             [field]: nextValue,[m
[36m@@ -395,7 +443,9 @@[m [mfunction AdminPage() {[m
         }[m
 [m
         if (errorMessage === "") {[m
[31m-            setCameraMessage("");[m
[32m+[m[32m            const total = Number(nextConfig.entry_lane_cameras) + Number(nextConfig.exit_lane_cameras);[m
[32m+[m[32m            setCameraMessage(total > 4 ? "🛡 Camera limit exceeded. A maximum of 4 cameras can be assigned across entry and exit lanes." : "");[m
[32m+[m[32m            setCameraMessageType(total > 4 ? "warning" : "success");[m
         }[m
     }[m
 [m
[36m@@ -410,9 +460,9 @@[m [mfunction AdminPage() {[m
         setCameraErrors(nextErrors);[m
 [m
         const hasError = Object.values(nextErrors).some((message) => Boolean(message));[m
[31m-        if (hasError) {[m
[32m+[m[32m        if (hasError || Number(cameraConfig.entry_lane_cameras) + Number(cameraConfig.exit_lane_cameras) > 4) {[m
             setCameraMessageType("warning");[m
[31m-            setCameraMessage(t.fixCameraErrors);[m
[32m+[m[32m            setCameraMessage(Number(cameraConfig.entry_lane_cameras) + Number(cameraConfig.exit_lane_cameras) > 4 ? "🛡 Camera limit exceeded. A maximum of 4 cameras can be assigned across entry and exit lanes." : t.fixCameraErrors);[m
             return;[m
         }[m
 [m
[36m@@ -525,6 +575,7 @@[m [mfunction AdminPage() {[m
             cash_enabled: Boolean(config?.cash_enabled),[m
             card_enabled: Boolean(config?.card_enabled),[m
             rate_per_minute: Number(config?.rate_per_minute ?? 1.67),[m
[32m+[m[32m            rate_unit: config?.rate_unit || "minute",[m
         });[m
     }[m
 [m
[36m@@ -621,6 +672,9 @@[m [mfunction AdminPage() {[m
         if (Boolean(currentSettings.automatic_entry) !== Boolean(savedSettings.automatic_entry)) {[m
             return false;[m
         }[m
[32m+[m[32m        if (Boolean(currentSettings.local_image_saving) !== Boolean(savedSettings.local_image_saving)) {[m
[32m+[m[32m            return false;[m
[32m+[m[32m        }[m
 [m
         return currentLevels.every((currentLevel, index) => {[m
             const savedLevel = savedLevels[index];[m
[36m@@ -818,6 +872,17 @@[m [mfunction AdminPage() {[m
 [m
     async function confirmGarageSettings() {[m
         if (settingsSubmitting) return;[m
[32m+[m[32m        if (garageSettings.mode === "tracking") {[m
[32m+[m[32m            setSettingsSubmitting("garage");[m
[32m+[m[32m            try {[m
[32m+[m[32m                const result = await saveGarageSettings(token, { mode: "tracking", level_count: 0, spaces_per_level: 0, levels: [], automatic_entry: Boolean(garageSettings.automatic_entry), local_image_saving: Boolean(garageSettings.local_image_saving) });[m
[32m+[m[32m                const saved = result.garage_settings;[m
[32m+[m[32m                setGarageSettings({ mode: "tracking", level_count: "0", spaces_per_level: "0", levels: [], automatic_entry: Boolean(saved.automatic_entry) });[m
[32m+[m[32m                setSavedGarageSettings({ mode: "tracking", level_count: "0", spaces_per_level: "0", levels: [], automatic_entry: Boolean(saved.automatic_entry) });[m
[32m+[m[32m                setGarageSettingsMessageType("success"); setGarageSettingsMessage("Plate tracking mode applied."); localStorage.setItem(GARAGE_SETTINGS_UPDATED_KEY, String(Date.now())); setConfirmationOpen(false); return;[m
[32m+[m[32m            } catch (err) { setGarageSettingsMessageType("warning"); setGarageSettingsMessage(err?.message || t.requestFailed); return; }[m
[32m+[m[32m            finally { setSettingsSubmitting(null); }[m
[32m+[m[32m        }[m
         const levelCountVal = garageSettings.level_count !== undefined[m
             ? garageSettings.level_count[m
             : (garageSettings.levels?.length ? String(garageSettings.levels.length) : "");[m
[36m@@ -852,21 +917,28 @@[m [mfunction AdminPage() {[m
         setSettingsSubmitting("garage");[m
         try {[m
             const result = await saveGarageSettings(token, {[m
[32m+[m[32m                mode: "parking",[m
                 level_count: count,[m
                 spaces_per_level: defaultSpaces,[m
                 levels: payloadLevels,[m
                 automatic_entry: Boolean(garageSettings.automatic_entry),[m
[32m+[m[32m                local_image_saving: Boolean(garageSettings.local_image_saving),[m
             });[m
             const savedSettings = result?.garage_settings || {[m
[32m+[m[32m                mode: "parking",[m
                 level_count: count,[m
                 spaces_per_level: defaultSpaces,[m
                 levels: payloadLevels,[m
                 automatic_entry: Boolean(garageSettings.automatic_entry),[m
[32m+[m[32m                local_image_saving: Boolean(garageSettings.local_image_saving),[m
[32m+[m[32m                local_image_saving: Boolean(garageSettings.local_image_saving),[m
             };[m
             setSavedGarageSettings({[m
[32m+[m[32m                mode: savedSettings.mode || "parking",[m
                 level_count: String(savedSettings.level_count),[m
                 spaces_per_level: String(savedSettings.spaces_per_level),[m
                 automatic_entry: Boolean(savedSettings.automatic_entry),[m
[32m+[m[32m                local_image_saving: Boolean(savedSettings.local_image_saving),[m
                 levels: (savedSettings.levels || payloadLevels).map((level) => ({[m
                     ...level,[m
                     spaces: String(level.spaces),[m
[36m@@ -874,9 +946,11 @@[m [mfunction AdminPage() {[m
             });[m
             setGarageSettings((current) => ({[m
                 ...current,[m
[32m+[m[32m                mode: savedSettings.mode || "parking",[m
                 level_count: String(count),[m
                 spaces_per_level: String(defaultSpaces),[m
                 automatic_entry: Boolean(savedSettings.automatic_entry),[m
[32m+[m[32m                local_image_saving: Boolean(savedSettings.local_image_saving),[m
                 levels: payloadLevels.map((l) => ({ ...l, spaces: String(l.spaces) })),[m
             }));[m
             setGarageSettingsMessageType("success");[m
[36m@@ -901,7 +975,7 @@[m [mfunction AdminPage() {[m
             <main className={`admin-shell admin-theme-${appliedTheme}`} dir={isUrdu ? "rtl" : "ltr"} lang={language}>[m
                 <header className="admin-header">[m
                     <a href="/" className="admin-logo">PARKING<span>OS</span></a>[m
[31m-                    <div className="admin-header-actions"><DisplayControls theme={theme} language={language} onLanguageChange={setLanguage} onThemeChange={setTheme} /><button type="button" className="theme-toggle" onClick={() => window.open("/", "_blank", "noopener,noreferrer")}>Open Garage</button><div className="admin-user">{adminName}<button type="button" onClick={signOut